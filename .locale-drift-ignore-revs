# Commits that touched translated pages without changing what they say.
# Staleness compares the last commit on an English page against the last commit
# on its translation, so a sweep that rewrites every locale file at once would
# otherwise mark every translation fresh and hide the real drift.
# One full SHA per line, same shape as .git-blame-ignore-revs.

# Prefixed every in-body link in zh-Hans and zh-Hant with its locale.
786581253456b3ae522da31593a1942fd977154f
