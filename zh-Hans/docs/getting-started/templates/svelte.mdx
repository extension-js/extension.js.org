---
title: "Svelte 浏览器扩展模板"
description: "生成一个 Svelte 浏览器扩展，自带单文件组件、编译后的运行时，以及 popup、options 与 content script 的预接线。"
keywords: ["extension.js", "template", "svelte", "browser extension"]
---

<img
  src="/images/examples/new-svelte/screenshot.png"
  alt="Svelte template preview"
  className="w-full rounded-xl border border-black/10 dark:border-white/10"
/>

## 创建项目

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=svelte
```

```bash pnpm
pnpx extension@latest create my-extension --template=svelte
```

```bash yarn
yarn dlx extension@latest create my-extension --template=svelte
```

```bash bun
bunx extension@latest create my-extension --template=svelte
```

```bash bun
bunx extension@latest create my-extension --template=svelte
```

</CodeGroup>

## 你将获得什么

编译期生成的 UI，使用单文件 `.svelte` 组件，运行时占用极小。非常适合每一个 KB 都很重要的 content script 与 popup。

模板包含了在扩展上下文中以 HMR 友好的方式编写 Svelte 所需的构建配置。

## 源码

在 GitHub 上浏览完整源码：[extension-js/examples/svelte](https://github.com/extension-js/examples/tree/main/examples/svelte)。
