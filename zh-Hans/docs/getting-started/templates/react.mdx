---
title: "React 浏览器扩展模板"
description: "搭建一个完整支持 Manifest V3、JSX,并预置 popup、options 和 content script 入口的 React 浏览器扩展。"
keywords: ["extension.js", "template", "react", "browser extension"]
---

<img
  src="/images/examples/new-react/screenshot.png"
  alt="React 模板预览"
  className="w-full rounded-xl border border-black/10 dark:border-white/10"
/>

## 创建项目

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=react
```

```bash pnpm
pnpx extension@latest create my-extension --template=react
```

```bash yarn
yarn dlx extension@latest create my-extension --template=react
```

```bash bun
bunx extension@latest create my-extension --template=react
```

```bash bun
bunx extension@latest create my-extension --template=react
```

</CodeGroup>

## 你将获得什么

一个基于组件的 UI 基线,完全兼容 Manifest V3(MV3)。popup、options 和 content script 都以 React 入口的形式提供,并接好了热模块替换(HMR)。修改组件无需手动重新加载扩展即可看到更新。

对于已经熟悉 React 的团队,这是最常见的起点。

## 源码

在 GitHub 上浏览完整源码:[extension-js/examples/react](https://github.com/extension-js/examples/tree/main/examples/react)。
