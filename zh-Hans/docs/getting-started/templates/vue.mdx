---
title: "Vue 浏览器扩展模板"
description: "搭建一个使用单文件组件并接好 Composition API 的 Vue 3 浏览器扩展,适用于 popup、options 和 content script 上下文。"
keywords: ["extension.js", "template", "vue", "browser extension"]
---

<img
  src="/images/examples/new-vue/screenshot.png"
  alt="Vue 模板预览"
  className="w-full rounded-xl border border-black/10 dark:border-white/10"
/>

## 创建项目

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=vue
```

```bash pnpm
pnpx extension@latest create my-extension --template=vue
```

```bash yarn
yarn dlx extension@latest create my-extension --template=vue
```

```bash bun
bunx extension@latest create my-extension --template=vue
```

```bash bun
bunx extension@latest create my-extension --template=vue
```

</CodeGroup>

## 你将获得什么

一个渐进式的配置,使用 `.vue` 单文件组件,适用于 popup、options 和 content script 上下文。模板处理好了 SFC 编译和重新加载行为,让你能专注于 UI。

适合那些已经在交付 Vue 应用,并希望扩展开发与现有工作流保持一致的团队。

## 源码

在 GitHub 上浏览完整源码:[extension-js/examples/vue](https://github.com/extension-js/examples/tree/main/examples/vue)。
