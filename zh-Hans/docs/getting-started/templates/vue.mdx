---
title: "Vue 浏览器扩展模板"
description: "脚手架一个 Vue 3 浏览器扩展,使用单文件组件与组合式 API,预先接线 popup、options 与内容脚本上下文。"
keywords: ["extension.js", "template", "vue", "browser extension"]
---

<img
  src="/images/examples/new-vue/screenshot.png"
  alt="Vue template preview"
  className="w-full rounded-xl border border-black/10 dark:border-white/10"
/>

## 创建项目

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=vue
```

```bash pnpm
pnpx extension@latest create my-extension --template=vue
```

```bash yarn
yarn dlx extension@latest create my-extension --template=vue
```

```bash bun
bunx extension@latest create my-extension --template=vue
```

```bash bun
bunx extension@latest create my-extension --template=vue
```

</CodeGroup>

## 你将获得什么

渐进式的设置,使用 `.vue` 单文件组件,已为 popup、options 与内容脚本上下文做好准备。模板会处理 SFC 编译与重载行为,让你可以专注于 UI。

对于已经在交付 Vue 应用、希望扩展开发体验与原生工作流一致的团队,这是一个很合适的选择。

## 源代码

在 GitHub 上浏览完整源代码:[extension-js/examples/vue](https://github.com/extension-js/examples/tree/main/examples/vue)。
