---
title: "TypeScript 浏览器扩展模板"
description: "脚手架一个 TypeScript 浏览器扩展,提供严格类型、Manifest V3 类型,以及预先接线的 popup、content 与 background 脚本入口。"
keywords: ["extension.js", "template", "typescript", "browser extension"]
---

<img
  src="/images/examples/new-typescript/screenshot.png"
  alt="TypeScript template preview"
  className="w-full rounded-xl border border-black/10 dark:border-white/10"
/>

## 创建项目

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=typescript
```

```bash pnpm
pnpx extension@latest create my-extension --template=typescript
```

```bash yarn
yarn dlx extension@latest create my-extension --template=typescript
```

```bash bun
bunx extension@latest create my-extension --template=typescript
```

```bash bun
bunx extension@latest create my-extension --template=typescript
```

</CodeGroup>

## 你将获得什么

在 popup、content 与 background 脚本中提供静态类型,并配置好 `chrome` 与 `browser` API 类型。让你能在编译期而不是浏览器控制台中捕获 manifest、消息传递和权限相关的 bug。

对于希望在扩展成长过程中获得强重构信心的团队,这是一个稳妥的选择。

## 源代码

在 GitHub 上浏览完整源代码:[extension-js/examples/typescript](https://github.com/extension-js/examples/tree/main/examples/typescript)。
