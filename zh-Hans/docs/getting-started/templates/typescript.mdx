---
title: "TypeScript 浏览器扩展模板"
description: "搭建一个带有严格类型、Manifest V3 类型,并已接好 popup、content 和 background script 入口的 TypeScript 浏览器扩展。"
keywords: ["extension.js", "template", "typescript", "browser extension"]
---

<img
  src="/images/examples/new-typescript/screenshot.png"
  alt="TypeScript 模板预览"
  className="w-full rounded-xl border border-black/10 dark:border-white/10"
/>

## 创建项目

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=typescript
```

```bash pnpm
pnpx extension@latest create my-extension --template=typescript
```

```bash yarn
yarn dlx extension@latest create my-extension --template=typescript
```

```bash bun
bunx extension@latest create my-extension --template=typescript
```

```bash bun
bunx extension@latest create my-extension --template=typescript
```

</CodeGroup>

## 你将获得什么

popup、content 和 background script 都有静态类型,并配置好了 `chrome` 与 `browser` API 的类型定义。让 manifest、消息传递和权限相关的 bug 在编译期就被发现,而不是在浏览器控制台中才暴露出来。

对于希望在扩展不断增长时拥有更强重构信心的团队来说,这是一个稳妥的选择。

## 源码

在 GitHub 上浏览完整源码:[extension-js/examples/typescript](https://github.com/extension-js/examples/tree/main/examples/typescript)。
