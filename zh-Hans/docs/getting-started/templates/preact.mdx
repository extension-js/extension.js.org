---
title: "Preact 浏览器扩展模板"
description: "使用与 React 兼容的 API 和 3kB 运行时搭建 Preact 浏览器扩展,适合体积敏感的 popup 和 content script。"
keywords: ["extension.js", "template", "preact", "browser extension"]
---

<img
  src="/images/examples/new-preact/screenshot.png"
  alt="Preact 模板预览"
  className="w-full rounded-xl border border-black/10 dark:border-white/10"
/>

## 创建项目

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=preact
```

```bash pnpm
pnpx extension@latest create my-extension --template=preact
```

```bash yarn
yarn dlx extension@latest create my-extension --template=preact
```

```bash bun
bunx extension@latest create my-extension --template=preact
```

```bash bun
bunx extension@latest create my-extension --template=preact
```

</CodeGroup>

## 你将获得什么

与 React 一致的组件模型,但只有 3kB 的运行时,并配置了标准的 `preact/compat` 别名,使大多数 React 库都能继续工作。当打包体积重要时,这是一个很好的选择,尤其适用于注入到他人页面中的 content script。

## 源码

在 GitHub 上浏览完整源码:[extension-js/examples/preact](https://github.com/extension-js/examples/tree/main/examples/preact)。
