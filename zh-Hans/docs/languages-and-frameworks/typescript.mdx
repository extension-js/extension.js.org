---
title: "为浏览器扩展使用 TypeScript"
description: "零配置构建 TypeScript 浏览器扩展。Extension.js 通过 Rspack 与 SWC 在后台脚本、内容脚本和页面中支持 TS。"
keywords:
  [
    "extension.js",
    "extensions",
    "browser",
    "typescript",
    "react",
    "rspack",
    "content script",
    "docs",
  ]
---

Extension.js 在每个扩展上下文中都能运行 TypeScript：后台、内容脚本、弹窗、选项页和侧边栏。默认使用 Rspack + SWC 管线。你不需要单独的 `tsc` 步骤、ts-loader 或额外的打包器规则。

## 适合使用 TypeScript 的场景

- 你希望在不同扩展上下文之间进行更安全的重构、保持更清晰的契约。
- 你在后台脚本、内容脚本和 UI 界面之间共享逻辑。
- 在使用 AI 生成的代码时，你需要可预测的 API 调用方式。

## 模板示例

### `new-typescript`

![new-typescript template screenshot](/images/examples/new-typescript/screenshot.png)

适合构建已配置好 TypeScript 默认值的新标签页扩展。

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=new-typescript
```

```bash pnpm
pnpx extension@latest create my-extension --template=new-typescript
```

```bash yarn
yarn dlx extension@latest create my-extension --template=new-typescript
```

```bash bun
bunx extension@latest create my-extension --template=new-typescript
```

```bash bun
bunx extension@latest create my-extension --template=new-typescript
```

</CodeGroup>

仓库：[extension-js/examples/new-typescript](https://github.com/extension-js/examples/tree/main/examples/new-typescript)

### `content-typescript`

![content-typescript template screenshot](/images/examples/content-typescript/screenshot.png)

适合将基于 TypeScript 的内容脚本注入到现有页面中。

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=content-typescript
```

```bash pnpm
pnpx extension@latest create my-extension --template=content-typescript
```

```bash yarn
yarn dlx extension@latest create my-extension --template=content-typescript
```

```bash bun
bunx extension@latest create my-extension --template=content-typescript
```

```bash bun
bunx extension@latest create my-extension --template=content-typescript
```

</CodeGroup>

仓库：[extension-js/examples/content-typescript](https://github.com/extension-js/examples/tree/main/examples/content-typescript)

## 自动生成的环境类型

对于 TypeScript 项目，Extension.js 会在项目根目录生成 `extension-env.d.ts` 文件，包含环境声明（浏览器/运行时全局、`EXTENSION_PUBLIC_*` 环境变量和打包器类型）。[`dev`](/docs/commands/dev) 和 [`build`](/docs/commands/build) 都会重新生成它，从而让编辑器类型和 CI 的 `tsc --noEmit` 保持一致。提交或加入 git 忽略均可 —— 都没问题；下次运行时会重新创建。

## 在现有扩展中使用

按以下步骤为现有扩展添加 TypeScript 支持。

### 安装

1. 将 TypeScript 作为开发依赖安装：

<PackageManagerTabs command="install -D typescript" />

2. 初始化 TypeScript 配置文件 `tsconfig.json`：

<PackageManagerTabs command="tsc --init" />

### 配置

#### TypeScript 检测与要求

Extension.js 会在 `package.json` 旁边查找 `tsconfig.json`。

- 如果存在 TypeScript 源文件但缺少 `tsconfig.json`，Extension.js 会抛出错误并要求你创建。
- 如果 Extension.js 通过依赖或配置检测到 TypeScript 但没有源文件，它可以为你创建一个基础的 `tsconfig.json`。

基础配置示例：

```json5
{
  compilerOptions: {
    allowJs: true,
    allowSyntheticDefaultImports: true,
    esModuleInterop: true,
    forceConsistentCasingInFileNames: true,
    isolatedModules: false,
    jsx: "react-jsx",
    lib: ["dom", "dom.iterable", "esnext"],
    moduleResolution: "node",
    module: "esnext",
    noEmit: true,
    resolveJsonModule: true,
    strict: true,
    target: "esnext",
    skipLibCheck: true,
  },
  exclude: ["node_modules", "dist"],
}
```

#### 自动类型

Extension.js 会在开发期间于项目根目录生成 `extension-env.d.ts`。该文件包含 Extension.js API 的类型声明以及浏览器 polyfill 类型。

```ts
// Required Extension.js types for TypeScript projects.
// This file is auto-generated and should not be excluded.
// If you need additional types, consider creating a new *.d.ts file and
// referencing it in the "include" array of your tsconfig.json file.
// See https://www.typescriptlang.org/tsconfig#include for more information.
/// <reference types="extension/types" />

// Polyfill types for browser.* APIs.
/// <reference types="extension/types/polyfill" />
```

#### 转译与类型检查

默认的打包器管线只编译 TypeScript，并不会在打包过程中运行完整的 `tsc` 类型检查。

建议的脚本：

```json
{
  "scripts": {
    "dev": "extension dev",
    "typecheck": "tsc --noEmit"
  }
}
```

## 后续步骤

- 了解 Extension.js 如何处理 [ECMAScript 模块](/docs/languages-and-frameworks/ecmascript-modules)。
- 探索 [Sass modules](/docs/languages-and-frameworks/sass) 等样式方案。
- 阅读 [浏览器扩展中的 JavaScript 与 TypeScript 文件](/docs/concepts/javascript-typescript-browser-extension-files)。
- 在各 [浏览器扩展框架](/docs/compare) 中对比 TypeScript 支持。

## 视频讲解

{/* VIDEO_PLACEHOLDER_TODO: Show TypeScript template setup, generated type env file, and a successful typecheck run. Intentionally hidden until a real demo video is available so it does not render in docs or appear in llms artifacts. */}
