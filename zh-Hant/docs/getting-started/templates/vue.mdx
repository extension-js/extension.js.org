---
title: "Vue 瀏覽器擴充功能範本"
description: "建立具備單檔元件與 Composition API 的 Vue 3 瀏覽器擴充功能,並已串接好 popup、options 與 content script context。"
keywords: ["extension.js", "template", "vue", "browser extension"]
---

<img
  src="/images/examples/new-vue/screenshot.png"
  alt="Vue template preview"
  className="w-full rounded-xl border border-black/10 dark:border-white/10"
/>

## 建立專案

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=vue
```

```bash pnpm
pnpx extension@latest create my-extension --template=vue
```

```bash yarn
yarn dlx extension@latest create my-extension --template=vue
```

```bash bun
bunx extension@latest create my-extension --template=vue
```

```bash bun
bunx extension@latest create my-extension --template=vue
```

</CodeGroup>

## 你會得到什麼

漸進式的設定,使用 `.vue` 單檔元件,可直接用於 popup、options 與 content script context。範本會處理 SFC 編譯與重新載入行為,讓你能專注在 UI 上。

對於已經在出貨 Vue 應用程式、希望擴充功能能延續同一套工作流的團隊來說,是非常合適的選擇。

## 原始碼

在 GitHub 上瀏覽完整原始碼:[extension-js/examples/vue](https://github.com/extension-js/examples/tree/main/examples/vue)。
