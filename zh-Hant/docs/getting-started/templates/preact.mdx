---
title: "Preact 瀏覽器擴充功能範本"
description: "建立採用 React 相容 API、僅 3kB runtime 的 Preact 瀏覽器擴充功能,專為對體積敏感的 popup 與 content script 而調校。"
keywords: ["extension.js", "template", "preact", "browser extension"]
---

<img
  src="/images/examples/new-preact/screenshot.png"
  alt="Preact template preview"
  className="w-full rounded-xl border border-black/10 dark:border-white/10"
/>

## 建立專案

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=preact
```

```bash pnpm
pnpx extension@latest create my-extension --template=preact
```

```bash yarn
yarn dlx extension@latest create my-extension --template=preact
```

```bash bun
bunx extension@latest create my-extension --template=preact
```

```bash bun
bunx extension@latest create my-extension --template=preact
```

</CodeGroup>

## 你會得到什麼

與 React 相同的元件模型,但只需 3kB 的 runtime,並已設定好標準的 `preact/compat` 別名,讓大多數 React 函式庫都能繼續運作。當打包大小很重要時——尤其是注入到他人頁面的 content script——這是很好的選擇。

## 原始碼

在 GitHub 上瀏覽完整原始碼:[extension-js/examples/preact](https://github.com/extension-js/examples/tree/main/examples/preact)。
