---
title: "React 瀏覽器擴充功能範本"
description: "建立完整支援 Manifest V3、具備 JSX 並可即刻出貨的 React 瀏覽器擴充功能,內含 popup、options 與 content script 進入點。"
keywords: ["extension.js", "template", "react", "browser extension"]
---

<img
  src="/images/examples/new-react/screenshot.png"
  alt="React template preview"
  className="w-full rounded-xl border border-black/10 dark:border-white/10"
/>

## 建立專案

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=react
```

```bash pnpm
pnpx extension@latest create my-extension --template=react
```

```bash yarn
yarn dlx extension@latest create my-extension --template=react
```

```bash bun
bunx extension@latest create my-extension --template=react
```

```bash bun
bunx extension@latest create my-extension --template=react
```

</CodeGroup>

## 你會得到什麼

以元件為基礎的 UI 基底,完整相容 Manifest V3 (MV3)。Popup、options 與 content script 都以 React 進入點出貨,並已串接好熱模組替換 (HMR)。編輯元件後即可即時更新,無需手動重新載入擴充功能。

對於已習慣使用 React 的團隊來說,這是最常見的起點。

## 原始碼

在 GitHub 上瀏覽完整原始碼:[extension-js/examples/react](https://github.com/extension-js/examples/tree/main/examples/react)。
