---
title: "Svelte 瀏覽器擴充功能範本"
description: "搭建一個使用單檔元件、已編譯執行時,並接好 popup、options 和 content script 的 Svelte 瀏覽器擴充功能。"
keywords: ["extension.js", "template", "svelte", "browser extension"]
---

<img
  src="/images/examples/new-svelte/screenshot.png"
  alt="Svelte 範本預覽"
  className="w-full rounded-xl border border-black/10 dark:border-white/10"
/>

## 建立專案

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=svelte
```

```bash pnpm
pnpx extension@latest create my-extension --template=svelte
```

```bash yarn
yarn dlx extension@latest create my-extension --template=svelte
```

```bash bun
bunx extension@latest create my-extension --template=svelte
```

```bash bun
bunx extension@latest create my-extension --template=svelte
```

</CodeGroup>

## 你會得到什麼

編譯期 UI,使用 `.svelte` 單檔元件,執行時佔用極小。非常適合每一千位元組都至關重要的 content script 和 popup。

範本包含了在擴充功能情境中進行 HMR 友善 Svelte 開發所需的建置設定。

## 原始碼

在 GitHub 上瀏覽完整原始碼:[extension-js/examples/svelte](https://github.com/extension-js/examples/tree/main/examples/svelte)。
