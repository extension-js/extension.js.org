---
title: "JavaScript 瀏覽器擴充功能範本"
description: "用 Extension.js CLI 建立 vanilla JavaScript 瀏覽器擴充功能，內含 manifest、popup 與 content script 的開箱即用設定。"
keywords: ["extension.js", "template", "javascript", "browser extension"]
---

<img
  src="/images/examples/javascript/screenshot.png"
  alt="JavaScript template preview"
  className="w-full rounded-xl border border-black/10 dark:border-white/10"
/>

## 建立專案

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=javascript
```

```bash pnpm
pnpx extension@latest create my-extension --template=javascript
```

```bash yarn
yarn dlx extension@latest create my-extension --template=javascript
```

```bash bun
bunx extension@latest create my-extension --template=javascript
```

```bash bun
bunx extension@latest create my-extension --template=javascript
```

</CodeGroup>

## 你會獲得什麼

純 ES modules 起點，不需編譯步驟。範本已串接好 `manifest.json`、popup 與 content script 進入點，你可以直接執行 `extension dev` 並立刻開始迭代。

當你希望以最小化的設定起步，或打算之後再依需求逐步加入工具時，這是合適的選擇。

## 原始碼

在 GitHub 瀏覽完整原始碼：[extension-js/examples/javascript](https://github.com/extension-js/examples/tree/main/examples/javascript)。
