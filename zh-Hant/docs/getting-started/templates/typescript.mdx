---
title: "TypeScript 瀏覽器擴充功能範本"
description: "搭建一個帶有嚴格型別、Manifest V3 型別,並已接好 popup、content 和 background script 進入點的 TypeScript 瀏覽器擴充功能。"
keywords: ["extension.js", "template", "typescript", "browser extension"]
---

<img
  src="/images/examples/new-typescript/screenshot.png"
  alt="TypeScript 範本預覽"
  className="w-full rounded-xl border border-black/10 dark:border-white/10"
/>

## 建立專案

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=typescript
```

```bash pnpm
pnpx extension@latest create my-extension --template=typescript
```

```bash yarn
yarn dlx extension@latest create my-extension --template=typescript
```

```bash bun
bunx extension@latest create my-extension --template=typescript
```

```bash bun
bunx extension@latest create my-extension --template=typescript
```

</CodeGroup>

## 你會得到什麼

popup、content 和 background script 都具備靜態型別,並已設定好 `chrome` 與 `browser` API 的型別定義。讓 manifest、訊息傳遞與權限相關的 bug 在編譯期就被抓出,而不是要等到瀏覽器主控台才浮現。

對於希望在擴充功能成長過程中擁有更強重構信心的團隊來說,是個穩妥的選擇。

## 原始碼

在 GitHub 上瀏覽完整原始碼:[extension-js/examples/typescript](https://github.com/extension-js/examples/tree/main/examples/typescript)。
