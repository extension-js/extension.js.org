---
title: "TypeScript 瀏覽器擴充功能範本"
description: "用 Extension.js 建立 TypeScript 瀏覽器擴充功能，搭配嚴格型別、Manifest V3 型別，以及預先連結的 popup、內容腳本與背景腳本進入點。"
keywords: ["extension.js", "template", "typescript", "browser extension"]
---

<img
  src="/images/examples/new-typescript/screenshot.png"
  alt="TypeScript template preview"
  className="w-full rounded-xl border border-black/10 dark:border-white/10"
/>

## 建立專案

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=typescript
```

```bash pnpm
pnpx extension@latest create my-extension --template=typescript
```

```bash yarn
yarn dlx extension@latest create my-extension --template=typescript
```

```bash bun
bunx extension@latest create my-extension --template=typescript
```

```bash bun
bunx extension@latest create my-extension --template=typescript
```

</CodeGroup>

## 你會得到什麼

popup、內容腳本與背景腳本均採用靜態型別，且設定了 `chrome` 與 `browser` API 的型別定義。在編譯時抓到 manifest、訊息傳遞與權限的 bug，而不是在瀏覽器主控台才發現。

對於想要隨著擴充功能成長保有重構信心的團隊而言，是穩健的選擇。

## 原始碼

在 GitHub 瀏覽完整原始碼：[extension-js/examples/typescript](https://github.com/extension-js/examples/tree/main/examples/typescript)。
