---
title: "用於建立擴充功能專案的 Create 指令"
description: "用一個 CLI 指令從官方範本建立全新的瀏覽器擴充功能專案。可選 React、Vue、Svelte、TypeScript 或原生 JS。"
keywords:
  [
    "browser",
    "typescript",
    "javascript",
    "react",
    "vue",
    "content script",
    "popup",
    "docs",
  ]
---

`create` 會根據所選範本建立檔案、設定與初始腳本,並可選擇性安裝相依套件。

## 何時使用 `create`

- 從零開始建立新擴充功能。
- 快速建立多個概念驗證(PoC)。
- 以一致的範本預設值,為團隊成員標準化專案上手流程。

## Create 指令功能

| 功能         | 你可以得到的能力                      |
| ------------ | ------------------------------------- |
| 範本建立     | 以官方範本和就緒的專案結構作為起點    |
| 安裝相依套件 | 建立後可選擇安裝所需套件              |
| 路徑彈性     | 以專案名稱或明確的資料夾路徑建立      |
| 快速上手     | 從空資料夾到可執行的擴充功能,只需幾步 |

## 用法

<CodeGroup>

```bash npm
npx extension@latest create <extension-name|extension-path> [options]
```

```bash pnpm
pnpx extension@latest create <extension-name|extension-path> [options]
```

```bash yarn
yarn dlx extension@latest create <extension-name|extension-path> [options]
```

```bash bun
bunx extension@latest create <extension-name|extension-path> [options]
```

```bash deno
deno run -A npm:extension@latest create <extension-name|extension-path> [options]
```

</CodeGroup>

## 引數與旗標

| 旗標                      | 別名 | 用途                                                            | 預設值       |
| ------------------------- | ---- | --------------------------------------------------------------- | ------------ |
| `[path or name]`          | -    | 要建立的專案資料夾/名稱。                                       | 必填         |
| `--template <name\|url>`  | `-t` | 用來建立專案的範本目錄名稱、GitHub URL 或 ZIP URL。             | `javascript` |
| `--install [boolean]`     | -    | 建立後安裝相依套件。                                            | `false`      |
| `--source <source>`       | -    | 標記這次 create 從哪裡發起(例如 `cli`)。僅記錄於匿名遙測。      | 未設定       |
| `--output <pretty\|json>` | -    | 結果格式。`json` 會在 stdout 印出一個 schema-1 信封。           | `pretty`     |

當你想使用預設的 JavaScript 起始範本時,完全不需指定 `--template`。只有在你想用 [官方範例](https://github.com/extension-js/examples/tree/main/examples) 中的其他技術棧時,才加上 `--template=<slug>`。`--template` 也接受 GitHub URL 或 ZIP URL,因此你可以從任何儲存庫建立專案。

範本目錄共有 6 組 53 個範本:starters、sidebar、content scripts、new tab、toolbar action 以及特殊資料夾。執行 `extension create --help` 可看到完整清單。預設的 `javascript` 範本內建於 CLI,因此建立時不需要任何網路請求。其他所有範本都會在建立時下載範本目錄的壓縮檔。

## 範本語料的版本固定

範本目錄的下載會固定在 examples 儲存庫的某一個不可變 commit 上。因此同一個版本的兩次建立會產生完全相同的位元組。有兩個環境變數可以覆寫這個固定:

- `EXTENSION_CREATE_TEMPLATE_REF` 指向另一個 ref。設為 `main` 可恢復浮動行為。
- `EXTENSION_CREATE_TEMPLATE_URL` 指向另一個完全不同的壓縮檔 URL。

每次建立都會在專案中寫入一個 `.extension-create.json` 來源紀錄檔。它記下 create 的版本、範本與來源,當範本來自範本目錄壓縮檔時還會記下解析出的 ref,讓範本漂移隨時可稽核。內建的 `javascript` 起始範本記錄的是 `"source": "bundled"`,而且沒有 ref。

## 用 `--output json` 取得機器可讀輸出

`--output json` 會在 stdout 印出一個 schema-1 信封,並把建立過程的進度訊息送到 stderr:

- 成功的執行會印出 `status: "created"` 訊框。它的 `value` 帶有 `projectPath`、`projectName`、`template` 與 `depsInstalled`。
- 失敗會印出 `ok: false` 以及一個 `error.code`:未知的範本名稱對應 `E_TEMPLATE_NOT_FOUND`,下載失敗對應 `E_NETWORK`,目標資料夾的問題對應 `E_DESTINATION_NOT_EMPTY` 或 `E_DESTINATION_NOT_WRITABLE`。

## 共用全域選項

也支援 [全域旗標](/docs/workflows/global-flags)。

## 指令範例

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=newtab-react
```

```bash pnpm
pnpx extension@latest create my-extension --template=newtab-react
```

```bash yarn
yarn dlx extension@latest create my-extension --template=newtab-react
```

```bash bun
bunx extension@latest create my-extension --template=newtab-react
```

```bash deno
deno run -A npm:extension@latest create my-extension --template=newtab-react
```

</CodeGroup>

<Frame>
  <iframe
    className="w-full aspect-video rounded-xl"
    src="https://www.youtube-nocookie.com/embed/czB7s95HuzA?rel=0"
    title="Extension.js: npx extension create scaffolds a project"
    loading="lazy"
    allow="encrypted-media; picture-in-picture; fullscreen"
    allowFullScreen
  />
</Frame>

<CodeGroup>

```bash npm
npx extension@latest create my-extension --install
```

```bash pnpm
pnpx extension@latest create my-extension --install
```

```bash yarn
yarn dlx extension@latest create my-extension --install
```

```bash bun
bunx extension@latest create my-extension --install
```

```bash deno
deno run -A npm:extension@latest create my-extension --install
```

</CodeGroup>

## 可用範本

<Tip>
  範本的完整與持續更新清單,請瀏覽 [examples
  儲存庫](https://github.com/extension-js/examples/tree/main/examples)。
</Tip>

<CardGroup cols={2}>
  <Card
    title="JavaScript(預設)"
    icon="js"
    href="https://github.com/extension-js/examples/tree/main/examples/javascript"
  >
    最精簡的起始範本。需要乾淨基線時使用。
  </Card>
  <Card
    title="TypeScript"
    icon="typescript"
    href="https://github.com/extension-js/examples/tree/main/examples/newtab-typescript"
  >
    預先設好 `tsconfig.json` 的型別化起始範本。
  </Card>
  <Card
    title="React"
    icon="react"
    href="https://github.com/extension-js/examples/tree/main/examples/newtab-react"
  >
    為 content scripts 與 popup 視圖串接好的 React UI。
  </Card>
  <Card
    title="Vue"
    icon="vuejs"
    href="https://github.com/extension-js/examples/tree/main/examples/newtab-vue"
  >
    內建單一檔案元件(SFC)支援的 Vue UI。
  </Card>
</CardGroup>

## 最佳實務

- 從符合你 UI/執行階段需求的範本開始,可減少後續設定上的偏移。
- 第一次執行時保持簡單,確認基本指令流程後再加入額外工具。
