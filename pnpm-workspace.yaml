# Single-package workspace so pnpm treats this directory as the workspace root.
# When this repo lives inside a parent monorepo, this prevents installs from
# joining the outer workspace and mutating or ignoring this lockfile.
packages:
  - .
