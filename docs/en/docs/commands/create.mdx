import {PackageManagerTabs} from '@theme'
import {AvatarImage} from '../../../../theme/components/avatar-image'
import {CarouselTemplates} from '../../../../theme/components/carousel-templates'

<AvatarImage icon="extension" />

# Create command

Bootstrap a new browser extension project from an official template in one command.

`create` scaffolds files, config, and starter scripts for the selected template and optionally installs dependencies.

## When to use `create`

- Start a new extension from scratch.
- Spin up multiple proof-of-concept ideas quickly.
- Standardize onboarding for teammates with consistent template defaults.

## Create command capabilities

| Capability           | What it gives you                                          |
| -------------------- | ---------------------------------------------------------- |
| Template scaffolding | Start from official templates with ready project structure |
| Dependency bootstrap | Install required packages automatically after scaffold     |
| Path flexibility     | Create by project name or explicit folder path             |
| Fast onboarding      | Move from empty folder to runnable extension quickly       |

## Usage

<PackageManagerTabs
  command={{
    npm: 'npx extension@latest create <extension-name|extension-path> [options]',
    pnpm: 'pnpx extension@latest create <extension-name|extension-path> [options]',
    yarn: 'yarn dlx extension@latest create <extension-name|extension-path> [options]'
  }}
/>

## Command options

| Flag                  | Alias | What it does                             | Default   |
| --------------------- | ----- | ---------------------------------------- | --------- |
| `[path or name]`      | -     | Project directory/name to create.        | required  |
| `--template <name>`   | `-t`  | Selects template slug.                   | `default` |
| `--install [boolean]` | -     | Installs dependencies after scaffolding. | `true`    |

## Shared global options

Also supports [global flags](../workflows/global-flags.mdx).

## Example commands

<PackageManagerTabs
  command={{
    npm: 'npx extension@latest create my-extension --template=react',
    pnpm: 'pnpx extension@latest create my-extension --template=react',
    yarn: 'yarn dlx extension@latest create my-extension --template=react'
  }}
/>

{/* VIDEO_PLACEHOLDER: Show creating a new extension from template, installing dependencies, and opening the generated project structure. */}
![Video placeholder: create command scaffold flow](https://placehold.co/960x540?text=Video+placeholder)

<PackageManagerTabs
  command={{
    npm: 'npx extension@latest create my-extension --template=default --install=false',
    pnpm: 'pnpx extension@latest create my-extension --template=default --install=false',
    yarn: 'yarn dlx extension@latest create my-extension --template=default --install=false'
  }}
/>

## Available templates

<CarouselTemplates />

## Best practices

- Start from a template that matches your UI/runtime needs to reduce setup drift.
- Keep the first run small, then add extra tooling after verifying baseline command flow.

## Next steps

- Develop your extension with [`dev`](/docs/commands/dev).
- Validate production behavior with [`start`](/docs/commands/start).
- Configure shared defaults in [`extension.config.js`](../features/extension-configuration.mdx).
