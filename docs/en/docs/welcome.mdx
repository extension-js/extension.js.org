import { PackageManagerTabs } from "@theme";
import { AvatarImage } from "../../../theme/components/avatar-image";

<AvatarImage icon="extension" />

# What is Extension.js?

Build browser extensions for Chrome, Edge, and Firefox with one modern workflow. Extension.js handles manifest compilation, browser-specific output, reload behavior, and packaging so you can focus on product features.

Use familiar web tooling like [TypeScript](./languages-and-frameworks/typescript.mdx), [React](./languages-and-frameworks/react.mdx), [Vue](./languages-and-frameworks/vue.mdx), and [Tailwind CSS](./integrations/tailwindcss.mdx). Keep direct access to native extension APIs.

## Pick your path

| If you are...                                  | Start here                                                                                                                                |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| Moving fast with AI tools and templates        | [Templates](./getting-started/templates.mdx) and [Get started immediately](./getting-started/immediately.mdx)                             |
| A seasoned web developer new to extensions     | [Create your first extension](./getting-started/create-your-first-extension.mdx) and [Manifest](./implementation-guide/manifest-json.mdx) |
| Shipping production extensions across browsers | [Cross-browser compatibility](./features/cross-browser-compatibility.mdx) and [Workflows overview](./workflows/index.mdx)                 |

## Choose the right command

| Goal                               | Command   | Example                                                         |
| ---------------------------------- | --------- | --------------------------------------------------------------- |
| Scaffold a project                 | `create`  | `npx extension@latest create my-extension --template=new-react` |
| Develop with watch mode            | `dev`     | `extension dev --browser=firefox`                               |
| Build production artifacts         | `build`   | `extension build --browser=chrome,firefox --zip`                |
| Build and launch production output | `start`   | `extension start --browser=edge`                                |
| Launch existing build output       | `preview` | `extension preview --browser=chrome`                            |

## Start a new extension

{/* VIDEO_DEMO_SOON: Existing Loom embed for creating a new extension. Keep this embed and replace only if explicitly requested. */}

<div
  style={{
    position: "relative",
    paddingBottom: "62.5%",
    height: 0,
  }}
>
  <iframe
    src="https://www.loom.com/embed/58e21900d693417db1e0e59c0a96c4b3?sid=80cf1003-7ed1-4f9d-a3fb-01c7876983ad"
    frameBorder="0"
    allowFullScreen
    style={{
      position: "absolute",
      top: 0,
      left: 0,
      width: "100%",
      height: "100%",
    }}
  ></iframe>
</div>

Use the `create` command to scaffold a new project and `--template` to start from a stack-specific baseline.

<PackageManagerTabs
  command={{
    npm: "npx extension@latest create <your-extension-name>",
    pnpm: "pnpx extension@latest create <your-extension-name>",
    yarn: "yarn dlx extension <your-extension-name>",
  }}
/>

For a list of all supported templates, check out the [Templates](/docs/getting-started/templates) page.

## Use Extension.js with an existing extension

{/* VIDEO_DEMO_SOON: Existing Loom embed for using Extension.js with an existing extension. Keep this embed and replace only if explicitly requested. */}

<div style={{ position: "relative", paddingBottom: "62.5%", height: 0 }}>
  <iframe
    src="https://www.loom.com/embed/c7ae4fc7cdfc47c39334c7efe3175dd9?sid=035792a7-aec9-4f1a-852b-47ca7166a539"
    allowFullScreen
    style={{
      position: "absolute",
      top: 0,
      left: 0,
      width: "100%",
      height: "100%",
    }}
  ></iframe>
</div>

If you already have an extension, install the `extension` package and wire scripts once. This keeps local development, testing, and release builds consistent.

### Step 1 - install the `extension` package as a `devDependency`

<PackageManagerTabs
  command={{
    npm: "npm install extension@latest --save-dev",
    pnpm: "pnpm add extension@latest --save-dev",
    yarn: "yarn add extension@latest --dev",
  }}
/>

### Step 2 - link your npm scripts to the `extension` commands

```json
{
  "scripts": {
    "build": "extension build",
    "dev": "extension dev",
    "start": "extension start"
  },
  "devDependencies": {
    "extension": "latest"
  }
}
```

That's it! You're all set.

- Run `npm run dev` for daily iteration and watch mode.
- Run `npm run start` for production build + immediate launch.
- Run `npm run build` for store-ready production artifacts.

## Best practices

- **Keep one command flow:** Use `create` -> `dev` -> `build` as your default loop.
- **Target browsers explicitly:** Validate with `--browser=chrome,firefox` before release.
- **Centralize defaults:** Put shared command/browser settings in `extension.config.js`.

## Next steps

- Learn how to [create your first extension](/docs/getting-started/create-your-first-extension).
- Explore built-in [templates](/docs/getting-started/templates).
- Add quality gates with [Playwright E2E](/docs/workflows/playwright-e2e).
- Strengthen reliability with [Troubleshooting](/docs/workflows/troubleshooting), [Security checklist](/docs/workflows/security-checklist), and [Performance playbook](/docs/workflows/performance-playbook).
