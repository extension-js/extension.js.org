---
title: "Browser targeting guide"
keywords: ["chrome", "firefox", "edge", "browser", "manifest", "docs", "browsers", "targeting"]
---

<AvatarBrowsers browsers={['chrome', 'firefox', 'edge']} />
Run one extension codebase across Chrome, Edge, Firefox, and custom binaries with explicit browser targeting workflows.

## What to read first

| Need                           | Read this                                              |
| ------------------------------ | ------------------------------------------------------ |
| Choose browser targets quickly | [Browsers available](/browsers/browsers-available)         |
| Customize launch behavior      | [Browser flags](/browsers/browser-flags)                   |
| Configure Firefox preferences  | [Browser preferences](/browsers/browser-preferences)       |
| Control profile isolation      | [Browser profile](/browsers/browser-profile)               |
| Run Brave or custom binaries   | [Running other browsers](/browsers/running-other-browsers) |

## Practical target strategy

1. Use named targets (`chrome`, `edge`, `firefox`) for daily checks.
2. Use comma-separated targets for release validation.
3. Use engine targets only when you need custom binaries.
4. Keep browser differences in browser-prefixed manifest fields.

## Next steps

- Learn manifest filtering in [Cross-browser compatibility](/features/cross-browser-compatibility).
- Configure browser-specific keys in [Browser-specific manifest fields](/features/browser-specific-fields).

## Video walkthrough

{/* VIDEO_PLACEHOLDER_TODO: Show targeting Chrome, Edge, and Firefox with one project and validating runtime differences. Intentionally hidden until a real demo video is available so it does not render in docs or appear in llms artifacts. */}
