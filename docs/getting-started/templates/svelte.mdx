---
title: "Svelte browser extension template"
description: "Scaffold a Svelte browser extension with single-file components, a compiled runtime, and ready-made sidebar and content script wiring."
keywords: ["extension.js", "template", "svelte", "browser extension"]
---

## Create the project

<CodeGroup>

```bash npm
npx extension@latest create my-extension --template=svelte
```

```bash pnpm
pnpx extension@latest create my-extension --template=svelte
```

```bash yarn
yarn dlx extension@latest create my-extension --template=svelte
```

```bash bun
bunx extension@latest create my-extension --template=svelte
```

```bash deno
deno run -A npm:extension@latest create my-extension --template=svelte
```

</CodeGroup>

## What you get

Compile-time UI with single-file `.svelte` components and a minimal runtime footprint. A good fit for content scripts and sidebar panels where every kilobyte counts.

The template scaffolds a sidebar panel and a content script, plus the build configuration needed for HMR-friendly Svelte authoring inside an extension context.

## Source

Browse the full source on GitHub: [extension-js/examples/svelte](https://github.com/extension-js/examples/tree/main/examples/svelte).

## Video walkthrough

<Frame>
  <iframe
    className="w-full aspect-video rounded-xl"
    src="https://www.youtube-nocookie.com/embed/jDjQNBN40_M?rel=0"
    title="Extension.js: Svelte template"
    loading="lazy"
    allow="encrypted-media; picture-in-picture; fullscreen"
    allowFullScreen
  />
</Frame>
