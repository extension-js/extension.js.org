{
 "batch": "b15",
 "pages": [
  {
   "page": "docs/workflows/build-an-ad-blocker.mdx",
   "claimsChecked": 11,
   "claimsVerifiedByRunning": 0,
   "findings": 0,
   "verdict": "accurate",
   "note": "New page, none of the example code was executed. Checked against sources: declarative_net_request rule_resources shape and the no-host-permission rule for block/allow actions per Chrome docs knowledge; updateDynamicRules and setExtensionActionOptions signatures per Chrome docs knowledge; declarativeNetRequestFeedback gating getMatchedRules; Firefox DNR support and its missing setExtensionActionOptions; Firefox keeping blocking webRequest in MV3. Extension.js claims verified by inspection: feature-json in programs/develop compiles rule_resources ruleset files (json-plugin.spec.ts covers declarative_net_request.ruleset), the action template exists in scripts/template-catalog.json and in the examples checkout with background + action popup. All five internal links resolve to existing pages. No numeric rule limits are claimed."
  },
  {
   "page": "docs/workflows/intercept-network-requests.mdx",
   "claimsChecked": 10,
   "claimsVerifiedByRunning": 0,
   "findings": 0,
   "verdict": "accurate",
   "note": "New page, code not executed. Checked against Chrome/MDN docs knowledge: webRequest non-blocking listeners with urls filters, details fields (requestId, tabId, statusCode, type), webRequestBlocking restricted to policy installs in Chrome MV3, webRequestAuthProvider for async onAuthRequired, chrome.devtools.network.onRequestFinished HAR entries with getContent/getHAR and no extra permission, Firefox MV3 keeping webRequestBlocking, Firefox MV3 host permissions being user opt-in, Firefox event pages. Verified by inspection: the devtools template exists in the catalog and its src layout (devtools/index.html + scripts.js creating a panel) matches the snippet. All four internal links resolve."
  },
  {
   "page": "docs/workflows/manage-downloads.mdx",
   "claimsChecked": 9,
   "claimsVerifiedByRunning": 0,
   "findings": 0,
   "verdict": "accurate",
   "note": "New page, code not executed. Checked against Chrome/MDN docs knowledge: downloads permission, download() filename relative-to-Downloads rule with subfolders allowed and no absolute or .. paths, cancel/pause/resume/erase existence, onChanged delta shape firing on state changes but not byte counts (hence the search() poll for bytesReceived/totalBytes), promise-based chrome.downloads.search in MV3, Firefox parity via browser.downloads and missing setUiOptions. The parallel-download section makes only conservative claims (browser-owned queue, per-server connection caps, no segmented downloads). Verified by inspection: action template slug and repo path exist. All four internal links resolve."
  },
  {
   "page": "docs/workflows/record-the-screen.mdx",
   "claimsChecked": 12,
   "claimsVerifiedByRunning": 0,
   "findings": 0,
   "verdict": "accurate",
   "note": "New page, code not executed. Checked against Chrome/MDN docs knowledge: getDisplayMedia needing a user gesture and no manifest permission, popup lifetime killing recordings, MediaRecorder ondataavailable/onstop flow, blob URLs working in documents but not service workers, tabCapture permission + getMediaStreamId({targetTabId}) + getUserMedia chromeMediaSource:'tab' constraint shape, offscreen documents with USER_MEDIA reason being Chromium-only, Firefox lacking tabCapture and offscreen while supporting getDisplayMedia. Extension.js claims verified by inspection of the special-folders-pages example: pages/ entries emit to dist/<browser>/pages/*.html and are opened via chrome.runtime.getURL('pages/...'), matching the recipe. Template slug and repo link exist. All internal links resolve."
  }
 ]
}
