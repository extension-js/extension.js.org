{
  "batch": "new-translations-2026-09-zh-Hant",
  "pages": [
    {
      "page": "zh-Hant/docs/migrate/from-wxt.mdx",
      "claimsChecked": 107,
      "findings": 2,
      "verdict": "minor-issues",
      "note": "All 7 fences diffed byte-for-byte against the English twin and identical, including the two install lines, the defineContentScript block, the manifest.json content_scripts block, the package.json scripts block, and `extension build --browser=chrome,firefox --zip`. The 7-row WXT-convention table carries the same left cells and the same manifest.json right cells in the same order; the one cell my differ flagged (`manifest: {...}` in `wxt.config.ts`) is a Chinese word-order swap with both tokens intact, not a value change. 9 headings in the same order at the same levels, 6 numbered steps. All 11 in-body links resolve to real zh-Hant files (compare/extension-js-vs-wxt, implementation-guide/content-scripts, features/environment-variables, concepts/manifest-v3, features/path-resolution, getting-started/templates, migrate/from-plasmo, migrate/from-crxjs, features/cross-browser-compatibility, features/reload-and-hmr). Every caveat survived: the polyfill split (`extension dev` applies it by default, `extension build` takes `--polyfill`, default off) is stated in all three places English states it, the MV2-first-finish-that-migration blocker is intact, the ctx-orphaning and chrome.runtime.id guard advice is intact, and the `.output/chrome-mv3` vs `dist/chrome` contrast is preserved. The page is 58 lines shorter than English only because the VIDEO_SPEC MDX comment is dropped, which is the established convention (0 of 140 zh-Hant and 0 of the zh-Hans pages carry one). One repair: \"Two WXT-specific helpers\" had been rendered 輔助功能, which reads as \"accessibility feature\" in Taiwan usage and collides with 擴充功能 for \"extension\"; changed to 輔助器, the form the existing zh-Hant corpus uses for helpers (content-scripts.mdx). Reviewed and accepted without change: the translation writes `<script>` where English says the unbackticked \"script tags\", an elaboration rather than a token mismatch; and 只以 Manifest V3 為目標 adds \"only\", which the following sentence in English already entails. Traditional throughout, fullwidth punctuation, no Simplified leakage. Also removed 只 from the Manifest V2 bullet: English says \"Extension.js targets Manifest V3\" with no \"only\", and the Simplified review dropped the same word, so the two locales now make the same claim as the English."
    },
    {
      "page": "zh-Hant/docs/implementation-guide/shadow-dom.mdx",
      "claimsChecked": 73,
      "findings": 1,
      "verdict": "minor-issues",
      "note": "All 9 fences byte-identical, including both full initial() mounts, the fetchCSS helper, the `#extension-root, [data-extension-root]` selector, the jsx teardown, and all 5 CodeGroup scaffold commands (npx/pnpx/yarn dlx/bunx/deno run -A, each ending --template=content-react). The bookkeeping-attribute table has the same 4 data rows and the same status values `mounted`, `executed`, `cleaned`, `mount-error`. 12 headings in the same order including all 4 CSS subheadings. The 16-template list is character-identical and in the same order. Caveats all present and none softened: the shadow root shields descendants but not the host so `all: initial !important` must stay, the `extension-js-devtools` reserved value, the attributes being stripped from production builds with the do-not-select-on-them instruction, the bundle-CSS hydration being conditional and stepping back once your own non-empty `<style>` appears, manifest `content_scripts[].css` never reaching a shadow root, and the two never-adopt guarantees (a host predating the mount, and another extension's host). All 4 links resolve, and both deep anchors were checked against the target file rather than assumed: #內容腳本中的網頁字型 is a real heading at css.mdx:133 and #撰寫合約 at content-scripts.mdx:125. One repair: the web-fonts paragraph rendered \"font faces\" as 字型族, which means font *family* and contradicts the page it links to; css.mdx writes 字型（font face）and 字型集 for font set, so this page now matches. Traditional throughout; the only halfwidth punctuation after CJK is inside the two CJK link anchors, not prose."
    },
    {
      "page": "zh-Hant/docs/implementation-guide/tabs.mdx",
      "claimsChecked": 74,
      "findings": 0,
      "verdict": "accurate",
      "note": "All 14 fences byte-identical, including the injected-permission array `[\"scripting\", \"tabs\", \"management\", \"storage\"]`, both dist/chromium/manifest.json blocks that make the dev-vs-build contrast, the untranslated English warning text in the plaintext fence, and all 6 CLI invocations (`extension inspect --list-tabs`, `extension inspect --tab 412`, the eval line with --context content --url \"https://example.com/*\", `extension reload --context content --tab 412`, `extension logs --tab 412`, `extension build`). Both tables intact: the permission table at 5 data rows with the same right-hand values, and the failure-message table at 4 data rows with all four messages kept verbatim in English. The trap is stated at full strength, not softened: the bolded **它不涵蓋 `tabs`。** keeps English's bold emphasis, the warning is still scoped to exactly `storage`, `scripting`, and `management`, and the compiles-clean/runs-clean-in-dev/breaks-when-packaged sequence survives. The manifest v2 difference (adds only tabs and storage, no host_permissions key) and the `extension storage` has-no---tab note are both present. The two-verbs-differ distinction (on reload `--tab` chooses the target, on logs it filters already-recorded events) is preserved exactly. All 5 links resolve. Uses 欄位 not 字段 and 元件 correctly; Traditional throughout."
    },
    {
      "page": "zh-Hant/docs/implementation-guide/cross-origin-requests.mdx",
      "claimsChecked": 56,
      "findings": 0,
      "verdict": "accurate",
      "note": "9 fences diffed; 8 are byte-identical and the ninth differs only by its single `//` comment being translated, which is the convention on this page's own siblings (messaging.mdx and storage.mdx, the two pages it links to, both translate comments). Critically the long CSP strings are untouched: both dist/chromium/manifest.json blocks match character for character including the trailing space after `object-src 'self'; ` and the full `connect-src 'self' ws://127.0.0.1:* ws://localhost:* http://127.0.0.1:* http://localhost:*; ` list. Both tables complete: the context table at 4 data rows keeping \"Yes, since Chrome 85\" for the isolated world and the two conditional \"No, when a host permission covers the URL\" cells as conditionals rather than flat noes, and the symptoms table at 5 data rows with all causes and fixes aligned. 11 headings in the same order including both `###` subsections. No caveat lost: host permissions do not lift CORS in a content script, a host permission removes the origin check but not the preflight, Extension.js does not validate host permissions against your code so a missing entry only shows at runtime, the dev server is local-only and never reaches production, and the direction-sensitive claim that a tight `connect-src` which forgets your API origin passes in development and fails in production is stated with both halves in the correct direction. The `--host 0.0.0.0` / `--public-host 192.168.1.20` pair and the 127.0.0.1 fallback are exact. All 5 links resolve. Traditional throughout."
    },
    {
      "page": "zh-Hant/docs/features/module-aliases.mdx",
      "claimsChecked": 71,
      "findings": 0,
      "verdict": "accurate",
      "note": "All 5 fences byte-identical: both tsconfig.json paths blocks (the baseUrl form with \"src/lib/*\" and the no-baseUrl form with \"./src/lib/*\", a distinction that only survives if the fence was diffed rather than eyeballed), the `@lib/badge.js` import, the extension.config.js resolve.alias hook, and the plaintext `Module not found: Can't resolve '@lib/badge.js'`. The toolchain-alias table has the same 6 data rows in the same order (Polyfill, React, Preact, Vue, Svelte, WebAssembly packages) with every package name intact including `react-dom/client`, `@vue/runtime-dom`, `@vue/runtime-core`, `@vue/shared`, `svelte/store`, `@ffmpeg/core`, `@imagemagick/magick-wasm`, `tesseract-wasm`. The 9-extension resolution list (.js .cjs .mjs .jsx .ts .mts .tsx .json .svelte) is complete and in order. Negative and exception claims all preserved: jsconfig.json is not read and renaming to tsconfig.json is the fix, no built-in `@/` `~/` or `src/` prefix exists, a tsconfig works in a pure-JavaScript project without installing `typescript`, framework aliases lose to yours because your resolve.alias merges last, and the one exception where `webextension-polyfill$` is applied after yours so an alias on that exact specifier does not take effect. All 5 links resolve. \"Specifier\" is rendered 識別字, which has precedent in the existing corpus (error-codes.mdx E_MODULE_NOT_FOUND), so not flagged. Traditional throughout."
    },
    {
      "page": "zh-Hant/docs/integrations/biome.mdx",
      "claimsChecked": 50,
      "findings": 0,
      "verdict": "accurate",
      "note": "All 13 fences byte-identical, including the 5-entry CodeGroup where each manager's flag form differs and had to survive intact (`npm install -D`, `pnpm add -D`, `yarn add -D`, `bun add -d` lowercase, `deno add -D npm:`), both biome.json blocks with the exact `[\"**\", \"!!**/dist\"]` and `[\"**\", \"!!**/dist\", \"!extension-env.d.ts\"]` include arrays, the vcs block, the package.json scripts block with lint/lint:fix/lint:ci, and all four npx biome invocations. The capability table has the same 4 data rows with `biome migrate eslint` and `biome migrate prettier` intact. 10 headings in the same order. Every claim about what the build does and does not do is preserved without softening: Extension.js does not read biome.json, does not run Biome, does not copy it into the packaged output, never watches it and never emits it to dist/; the closed set of root files it does read is listed with the same members; and the counterweight detail that `extension build --zip-source` does put biome.json into the source archive is kept, not dropped. The \"there is no Biome template\" section keeps both halves (templates exist for ESLint, Prettier and Stylelint, none for Biome). The `biome ci` never-edits-a-file guarantee and the run-ci-not---write CI advice survive. All 4 links resolve. Traditional throughout."
    },
    {
      "page": "zh-Hant/docs/languages-and-frameworks/extension-api-types.mdx",
      "claimsChecked": 70,
      "findings": 2,
      "verdict": "minor-issues",
      "note": "All 15 fences byte-identical, including both triple-slash reference directives, the two-line tsc error output with its exact line/column offsets and TS2304/TS2503 codes, the tsconfig.json include/exclude block, and both 5-entry CodeGroups (@types/chrome and @types/webextension-polyfill) with each manager's flag form correct. Both tables intact: the reference table at 3 data rows and the symptoms table at 4 data rows, with all four error strings kept verbatim in English. 7 headings in the same order. Claims preserved: SWC strips types and never checks them so the build succeeded even before the install, extension-env.d.ts is regenerated on every run and must not be edited, `@types/chrome` is neither installed by Extension.js nor declared by the templates, and `tsc --noEmit` belongs in CI because the build does not fail on type errors. Two repairs. First, the section heading read 當你改寫 browser.* 時 (\"when you rewrite\"), where English is \"When you write browser.* instead\"; changed to 當你改用, and I confirmed nothing links to that anchor before touching it. Second, the include-list paragraph said Extension.js 寫下這份檔案 (\"writes this file\"), which points at the scaffolded tsconfig.json shown in the fence immediately above and therefore contradicted the very next clause saying that file has no include array; English says \"a tsconfig.json\", so it now reads 寫出一份 `tsconfig.json`. All 4 links resolve. The link label 跨瀏覽器相容 without 性 was checked and left alone: it has precedent in three established zh-Hant pages (messaging, storage, web-accessible-resources). Traditional throughout."
    },
    {
      "page": "zh-Hant/docs/languages-and-frameworks/bun.mdx",
      "claimsChecked": 54,
      "findings": 0,
      "verdict": "accurate",
      "note": "All 7 fences byte-identical, and two of them carry values that would break silently if translated: the plaintext next-steps block is left in English exactly as the CLI prints it (Next steps / cd my-extension / bun install / bun dev / \"Run the extension in a fresh browser profile.\"), and the long refusal message is verbatim, keeping the specific numbers 1.2.13, 22.6.0 and 22.12 and the point of the message, that the installed Node.js is not the problem. Also identical: `\"packageManager\": \"bun@1.2.13\"`, the engines line `\"engines\": {\"node\": \">=22.12\"}` as written, and `bunx --bun extension@latest --version`. The can-I-use-Bun table has the same 4 data rows with the fourth still answering No/必須用 Node rather than being softened. 8 headings in the same order. The split is stated without drift: yes for the project, no for the CLI process, both must be installed, plain `bunx` respects the `#!/usr/bin/env node` shebang so the process runs on Node while Bun does resolution and caching. Detection is exact on all three signals (bun.lock or bun.lockb, a packageManager field naming bun, npm_config_user_agent), the supported list is npm/pnpm/yarn/bun/deno, and the PATH probe order is still the shorter pnpm, yarn, bun. All three caveats survive, including `--ignore-scripts` on the automatic install so a dependency postinstall does not run, Bun.file not belonging in extension code, and imported templates shipping without a lockfile because bun.lock and bun.lockb are stripped. All 3 links resolve. Traditional throughout."
    }
  ]
}
