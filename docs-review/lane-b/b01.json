{
 "batch": "b01",
 "pages": [
  {
   "page": "docs/ai-access.mdx",
   "claimsChecked": 8,
   "claimsVerifiedByRunning": 6,
   "findings": 0,
   "verdict": "accurate",
   "note": "Published CLI under test is extension@4.1.3. Page claims the hosted MCP endpoint, llms.txt/llms-full.txt endpoints, and that `extension --ai-help --output json` emits commands/flags/contract paths; verified endpoints with curl (llms.txt 200, llms-full.txt 200, /mcp 405 for GET which is correct for a POST-only MCP endpoint) and ran --ai-help --output json (has version, commands, globalOptions, templates, capabilities.readyContract with readyPath/eventsPath). Cross-links to driving-the-cli and contracts/index resolve."
  },
  {
   "page": "docs/commands/create.mdx",
   "claimsChecked": 14,
   "claimsVerifiedByRunning": 12,
   "findings": 1,
   "verdict": "minor-issues",
   "note": "Ran `extension create --help` and a real `create --output json` scaffold: every flag, alias, and default matches; the schema-1 `status:\"created\"` envelope with projectPath/projectName/template/depsInstalled matches; .extension-create.json is written; the four error codes and both EXTENSION_CREATE_TEMPLATE_* env vars exist in the shipped bundle. Only the template count is stale."
  },
  {
   "page": "docs/commands/start.mdx",
   "claimsChecked": 20,
   "claimsVerifiedByRunning": 16,
   "findings": 1,
   "verdict": "minor-issues",
   "note": "Ran `extension start --help`, a real `start --no-browser --output json` build, a `--browser safari` rejection, and a `--wait` remote-URL rejection. Flag table, defaults, dist/extension-js/<browser>/ready.json path, `status:\"started\"` frame, and E_ARGS for remote+--wait all check out; the Safari error code documented is wrong."
  },
  {
   "page": "docs/contracts/lifecycle-stream.mdx",
   "claimsChecked": 13,
   "claimsVerifiedByRunning": 9,
   "findings": 0,
   "verdict": "accurate",
   "note": "Ran `EXTENSION_OUTPUT=ndjson extension dev ./scratch-ext --browser chromium --no-browser` and got exactly the documented starting/compiled/ready frames carrying command, browser, distPath, pid, port, readyPath, eventsPath, runId, instanceId, toolchainVersion, requestedPort, assets, durationMs. Verified the 2000-character output cap (MAX_OUTPUT_CHARS = 2000 in extension-develop/dist/839.mjs) and the presence of E_FIRST_COMPILE, E_COMPILE, E_READY_ERROR_STATUS, readyCode, E_PROFILE_LOCKED, E_BROWSER_LAUNCH, browserExitedAt."
  },
  {
   "page": "docs/features/index.mdx",
   "claimsChecked": 12,
   "claimsVerifiedByRunning": 12,
   "findings": 1,
   "verdict": "minor-issues",
   "note": "Pure navigation page. Verified all 12 cross-references resolve to real .mdx files on disk; all present. The only issue is a 'Video walkthrough' heading with nothing under it."
  },
  {
   "page": "docs/getting-started/templates/javascript.mdx",
   "claimsChecked": 5,
   "claimsVerifiedByRunning": 4,
   "findings": 1,
   "verdict": "significant-issues",
   "note": "Scaffolded the actual javascript template with the published CLI and inspected the tree: the template is a sidebar/side-panel starter (background + content + sidebar), not the popup starter the page describes. Screenshot asset and GitHub source link both exist."
  },
  {
   "page": "docs/implementation-guide/icons.mdx",
   "claimsChecked": 11,
   "claimsVerifiedByRunning": 6,
   "findings": 1,
   "verdict": "minor-issues",
   "note": "Verified the `action` template exists in the shipped catalog, the screenshot asset exists, and both cross-links resolve. Built a project with icons and compared emitted paths against the documented 'Output path' block, which does not match how icons are actually emitted."
  },
  {
   "page": "docs/implementation-guide/sandbox-pages.mdx",
   "claimsChecked": 6,
   "claimsVerifiedByRunning": 2,
   "findings": 0,
   "verdict": "accurate",
   "note": "Verified Extension.js has real sandbox handling (plugin-web-extension/feature-manifest/manifest-overrides/common/sandbox.ts, feature-html/index.ts references sandbox) and that all four See-also cross-links resolve. The chrome.*-unavailable / unique-origin behavior of MV3 sandbox pages is browser behavior I did not launch a browser to confirm; the background.type ESM claim matches the built manifest (no type emitted for a non-ESM-import background)."
  },
  {
   "page": "docs/integrations/prettier.mdx",
   "claimsChecked": 5,
   "claimsVerifiedByRunning": 3,
   "findings": 0,
   "verdict": "accurate",
   "note": "Verified `new-config-prettier` is a real catalog template in the shipped CLI, its screenshot asset exists, and the eslint/stylelint cross-links resolve. The rest is standard Prettier usage outside the Extension.js pipeline, which the page correctly says it is."
  },
  {
   "page": "docs/languages-and-frameworks/react.mdx",
   "claimsChecked": 13,
   "claimsVerifiedByRunning": 9,
   "findings": 1,
   "verdict": "minor-issues",
   "note": "Verified new-react, content-react, new-react-router are real catalog templates, their screenshots exist, and both cross-links resolve. Ran a React-dependency project through `extension dev --no-browser`. Read plugin-js-frameworks/js-tools/react.ts: refresh deps are resolved via the *WithoutInstall* helpers which throw an install hint, so the 'Extension.js installs them' line is wrong and contradicts the page's own earlier paragraph."
  },
  {
   "page": "docs/workflows/ci-templates.mdx",
   "claimsChecked": 15,
   "claimsVerifiedByRunning": 11,
   "findings": 1,
   "verdict": "minor-issues",
   "note": "Verified `extension open --help` and `extension storage --help` accept every flag the YAML samples use, that dev has --allow-control/--allow-eval/--wait/--wait-timeout, that the --wait-format deprecated alias is still in the shipped bundle, that ready.json lands at dist/extension-js/<browser>/ready.json, that browserPid exists in the shipped contract code, and that all four cross-links resolve. Only the .github/workflows/e2e.yml claim is wrong for a reader's own project."
  }
 ]
}