{
 "batch": "b03",
 "pages": [
  {
   "page": "docs/browsers/browser-preferences.mdx",
   "claimsChecked": 11,
   "claimsVerifiedByRunning": 9,
   "findings": 0,
   "verdict": "accurate",
   "note": "Published CLI under test is extension@4.1.3 (./node_modules/.bin/extension --version). Verified preferences config keys against extension-develop/dist/types.d.ts (browser.<target>.preferences and commands.dev|start|preview.preferences all exist), Firefox user.js write + getPreferences merge, seedChromiumPreferences (Default/Preferences, deepMergePreferences, existsSync early-return, chrome vs edge baseline), EXTENSION_BROWSER_FLAGS appended after config browserFlags, EXTENSION_USE_SYSTEM_PROFILE, and getDarkModeDefaults (--force-dark-mode for chromium, ui.systemUsesDarkTheme/layout.css.prefers-color-scheme.content-override/devtools.theme for gecko). All accurate."
  },
  {
   "page": "docs/commands/doctor.mdx",
   "claimsChecked": 14,
   "claimsVerifiedByRunning": 14,
   "findings": 0,
   "verdict": "accurate",
   "note": "Ran `extension doctor` and `extension doctor --output json` in a session-less project: 7 checks in the documented order and names, documented glyphs, skip cascade, exit 1, schema-1 envelope with value on the failure verdict, E_SESSION_NOT_FOUND for ready-contract. Confirmed the full CHECK_CODES map, resolveDoctorBrowser (single session wins, else chromium, else first alphabetically, else chromium fallback), the session-resolution warn, and EXECUTOR_ATTACH_GRACE_MS = 10000 in the shipped bundle. Everything matches."
  },
  {
   "page": "docs/commands/uninstall.mdx",
   "claimsChecked": 10,
   "claimsVerifiedByRunning": 9,
   "findings": 0,
   "verdict": "accurate",
   "note": "Ran `extension uninstall --help` and `extension uninstall --where --output json` (printed status \"located\" with value.paths). Confirmed --all/--where/--browser/--output flags, EXT_BROWSERS_CACHE_DIR in the shipped bundle, E_BROWSER_UNINSTALL in the code table, and installTargets('all') === ['chrome','chromium','edge','firefox'] backing the `--all` set claim. No destructive uninstall was run."
  },
  {
   "page": "docs/contracts/result-envelope.mdx",
   "claimsChecked": 12,
   "claimsVerifiedByRunning": 10,
   "findings": 1,
   "verdict": "minor-issues",
   "note": "Envelope field table, error-object fields (name/engine/hint/refs) and the truncated/hint optionals all match extension-develop/dist/contract/envelope.schema.json ($id confirmed). codes.json has the documented folded/legacy maps, the ./contract/* export exists, and all named golden fixtures are present. Only the pasted capabilities example is stale."
  },
  {
   "page": "docs/features/path-resolution.mdx",
   "claimsChecked": 18,
   "claimsVerifiedByRunning": 16,
   "findings": 2,
   "verdict": "significant-issues",
   "note": "Built two hand-written projects (MV3/chrome and MV2/firefox) and read the emitted manifests to check every row of the manifest output table. 13 of 14 rows are exactly right; chrome_url_overrides.newtab is wrong. Extension-mapping table (.ts/.tsx/.js/.jsx to .js, .njk/.nunjucks/.html to .html, .scss/.sass/.less/.css to .css) matches getFilename in the shipped rspack config, and public/ assets emitted to output root as documented."
  },
  {
   "page": "docs/getting-started/templates/react.mdx",
   "claimsChecked": 6,
   "claimsVerifiedByRunning": 4,
   "findings": 2,
   "verdict": "significant-issues",
   "note": "`react` is a real template (present in `extension --ai-help --output json` templates.names and in the examples repo), but the page describes a different extension than the template actually ships, and embeds a different template's screenshot."
  },
  {
   "page": "docs/implementation-guide/javascript.mdx",
   "claimsChecked": 12,
   "claimsVerifiedByRunning": 9,
   "findings": 1,
   "verdict": "minor-issues",
   "note": "Verified splitChunks:false, resolve.mainFields ['browser','module','main'] and builtin:swc-loader in extension-develop/dist/0~rspack-config.mjs, and that both named templates exist. The `content` template description is right; the `javascript` template description is not."
  },
  {
   "page": "docs/implementation-guide/themes.mdx",
   "claimsChecked": 9,
   "claimsVerifiedByRunning": 7,
   "findings": 1,
   "verdict": "minor-issues",
   "note": "Read parseHexThemeColor/parseCssThemeColor/collectThemeValueIssues/patchChromiumThemeColors/isStaticTheme in the shipped bundle. 3/4/6/8-digit hex accepted, #24283bcc computes to [36,40,59,0.8] exactly as documented, tints require 3 numbers, validation is gated to chromium+webkit targets, webkit emits a warning and returns early, and THEME_DISQUALIFYING_KEYS is the same 15-key list in the same order. Only the pass-through sentence overstates."
  },
  {
   "page": "docs/integrations/stylelint.mdx",
   "claimsChecked": 6,
   "claimsVerifiedByRunning": 4,
   "findings": 1,
   "verdict": "minor-issues",
   "note": "Template `new-config-stylelint` exists and ships stylelint + stylelint-config-standard-scss as devDependencies, matching the install instructions. The generic stylelint/prettier usage is external tooling and correct. The 'Detection notes' claim has no support in the shipped CLI."
  },
  {
   "page": "docs/languages-and-frameworks/svelte.mdx",
   "claimsChecked": 11,
   "claimsVerifiedByRunning": 8,
   "findings": 1,
   "verdict": "significant-issues",
   "note": "Confirmed maybeUseSvelte in the shipped rspack config: svelte-loader as an optional contract package, hotReload/compilerOptions.dev gated on development mode, and aliases for svelte, svelte/store, svelte/reactivity (plus svelte/legacy). Both templates exist. The two code samples use the removed Svelte 4 component API."
  },
  {
   "page": "docs/workflows/driving-the-cli.mdx",
   "claimsChecked": 15,
   "claimsVerifiedByRunning": 13,
   "findings": 1,
   "verdict": "minor-issues",
   "note": "Ran `extension capabilities` bare (it does default to JSON), `extension --ai-help --output json` (payload keys version/commands/globalOptions/templates/capabilities/examples all present with the documented sub-keys), and --help for dev/storage/reload/open/logs/eval to confirm --allow-control, --allow-eval (0600 token), --wait, --key, --context and --output ndjson. Only the pasted capabilities example is stale."
  }
 ]
}