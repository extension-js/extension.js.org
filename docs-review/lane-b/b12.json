{
 "batch": "b12",
 "pages": [
  {
   "page": "docs/implementation-guide/native-messaging.mdx",
   "claimsChecked": 12,
   "claimsVerifiedByRunning": 2,
   "findings": 0,
   "verdict": "accurate",
   "note": "New page. Ran the documented Node.js length-prefixed stdio host against a driver process and got the framed echo reply back, and ran published `extension dev --help`, whose --profile text confirms that `--profile false` targets the default user profile. Checked in source (not run): the nativeMessaging permission passes through the built manifest with no product-side handling (only the Safari webkit:permissions filter specs touch it), managed profiles launch via --user-data-dir under dist/extension-js/profiles/<browser>-profile (browsers/run-chromium/chromium-launch/browser-config.ts, browsers-lib/resolve-profile.ts), normalizeProfileOption maps the string 'false' to the system profile, and the firefox: manifest prefix is a real documented feature (docs/features/multi-platform-builds.mdx). Host manifest fields, per-OS install locations, the 1 MB / 4 GB caps, and Firefox allowed_extensions/gecko.id requirements come from Chrome and MDN documentation knowledge, not from a live browser run. All four internal links resolve to real pages."
  },
  {
   "page": "docs/debugging/source-maps.mdx",
   "claimsChecked": 10,
   "claimsVerifiedByRunning": 1,
   "findings": 0,
   "verdict": "accurate",
   "note": "New page. Ran published `extension build --help`, which confirms --mode <development|production|none> with a production default. Checked in source (not run): devtool defaults in programs/develop/rspack-config.ts lines 413-418 (production false, MV3 cheap-source-map, MV2 eval-cheap-source-map), dev mode pinned to 'development' (dev-server/index.ts line 688), the extension.config.js config hook applied in both dev (dev-server/index.ts lines 701-702) and build with browser-runner plugins stripped (command-build.ts lines 255-262), function and object-merge config forms (lib/config-loader.ts loadCustomConfig), and the dev-only MV2 'unsafe-eval' CSP patch with no MV3 counterpart (feature-manifest/steps/apply-dev-defaults-lib/patch-csp.ts). The devtool taxonomy (module/eval/inline/hidden variants) is standard rspack behavior I did not build a project to observe. All three internal links resolve."
  }
 ]
}
