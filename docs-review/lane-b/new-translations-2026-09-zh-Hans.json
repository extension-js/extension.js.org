{
  "batch": "new-translations-2026-09-zh-Hans",
  "pages": [
    {
      "page": "zh-Hans/docs/migrate/from-wxt.mdx",
      "claimsChecked": 34,
      "findings": 1,
      "verdict": "minor-issues",
      "note": "All 7 code fences byte-identical to English by programmatic diff, including the two install lines, the defineContentScript sample, the manifest.json content_scripts block, the package.json scripts block, and both extension build/dev invocations with --browser=chrome,firefox --zip. The 7-row WXT-convention to manifest.json table matches cell for cell (action/options_ui/chrome_url_overrides/background/content_scripts plus the wxt.config.ts manifest option and the <meta name=\"manifest.*\"> row). 9 headings in order, and they follow the from-plasmo convention already in zh-Hans (哪些会变，哪些不变 / 第 N 步 / 常见坑 / 另请参阅). All 12 links resolve: 11 zh-Hans files exist on disk, 1 external to wxt.dev. The two inline tokens the English carries and the translation does not (migrate-wxt, extension migrate) live only inside the {/* VIDEO_SPEC */} JSX comment, which zh-Hans drops on from-plasmo and from-crxjs too, so the omission follows the established convention rather than losing a claim. Caveats survive: ctx invalidation must be replaced with addEventListener plus a chrome.runtime.id guard, createShadowRootUi/createIntegratedUi have no equivalent, extension dev applies the polyfill by default while extension build needs --polyfill (stated in all three places the English states it), and dist/chrome + dist/firefox replace .output/chrome-mv3. One defect fixed: the Manifest V2 gotcha read \"Extension.js 只面向 Manifest V3\", turning the English \"Extension.js targets Manifest V3\" into an absolute that the tabs page in this same batch contradicts (development still adds tabs and storage for a manifest v2 project). 只 removed."
    },
    {
      "page": "zh-Hans/docs/implementation-guide/shadow-dom.mdx",
      "claimsChecked": 28,
      "findings": 0,
      "verdict": "accurate",
      "note": "All 9 fences byte-identical, including both src/content/scripts.js mounts, the fetchCSS helper, the plaintext selector #extension-root, [data-extension-root], the jsx teardown, and all 5 CodeGroup scaffold commands (npx/pnpx/yarn dlx/bunx/deno run -A, each with --template=content-react). 45 inline tokens match as a multiset, so the full 16-template list is verbatim. The 4-row data-extjs-reinject-* table keeps its owner/key/build/status rows with the status values mounted, executed, cleaned, mount-error. 12 headings in order. Every caveat survives: Extension.js does not create the shadow root for you, there is no class-based host form, the attribute value is a convention not a requirement, extension-js-devtools is reserved for the bundled overlay, the bookkeeping attributes are stripped from production builds and must not be selected against, bundle-CSS hydration is conditional and steps back once your own <style> holds text, content_scripts[].css never reaches a shadow root, @font-face never applies inside one, reload reinjects the whole bundle rather than swapping modules, and a host that predates the mount or belongs to another extension is never adopted. All 5 links resolve, including the two CJK anchors: #内容脚本中的网页字体 is a real heading in zh-Hans/docs/implementation-guide/css.mdx and #编写契约 is a real heading in zh-Hans/docs/implementation-guide/content-scripts.mdx. Simplified throughout."
    },
    {
      "page": "zh-Hans/docs/implementation-guide/tabs.mdx",
      "claimsChecked": 33,
      "findings": 0,
      "verdict": "accurate",
      "note": "All 14 fences byte-identical, including both dist/chromium/manifest.json samples (the [\"storage\"] build one and the [\"scripting\", \"tabs\", \"management\", \"storage\"] dev one), every extension inspect/eval/reload/logs invocation with --list-tabs, --tab 412, --context content and --url \"https://example.com/*\", and the plaintext chrome.management warning, which is CLI output and correctly stays in English. Both tables intact: 5 permission rows (none for tab ids, tabs-or-host for tab.url/title/favIconUrl, tabs-or-host for a url filter, activeTab after a click, scripting plus host for injection) and 4 failure-message rows with their exact English message strings. 8 headings in order. The load-bearing caveat is fully preserved and not softened: the warning covers storage, scripting and management and **does not cover tabs**, so a project calling chrome.tabs compiles clean, runs clean in dev, and breaks in the packaged extension. Also preserved: manifest v2 gets tabs and storage plus match patterns because it has no host_permissions key, none of the injection reaches production, --tab takes a numeric id and nothing else, reload's --tab chooses a target while logs' --tab filters recorded events, extension storage has no --tab at all, and the restricted-page row applies to chrome:// and the Web Store. All 5 links resolve under zh-Hans."
    },
    {
      "page": "zh-Hans/docs/implementation-guide/cross-origin-requests.mdx",
      "claimsChecked": 26,
      "findings": 0,
      "verdict": "accurate",
      "note": "9 fences diffed: 8 byte-identical and 1 differing only by its inline comment, which the translation renders as // 让消息通道保持打开，等待这次异步答复。 That matches the house convention, since zh-Hans/docs/implementation-guide/messaging.mdx translates its four in-fence comments the same way. The two dev-manifest CSP fences are exact, including the trailing spaces and the full connect-src 'self' ws://127.0.0.1:* ws://localhost:* http://127.0.0.1:* http://localhost:*; string, as are the http fence Access-Control-Allow-Origin: *, the plaintext remote-script message, and both extension dev --host 0.0.0.0 / --public-host 192.168.1.20 commands. Both tables keep their rows: 4 context rows (background and extension pages exempt when a host permission covers the URL, isolated world subject to CORS since Chrome 85, MAIN world subject) and 5 symptom/cause/fix rows. 11 headings in order. Every caveat survives: host permissions do not lift CORS in a content script, Extension.js does not validate host permissions against URLs in your code so a missing entry only shows at runtime, a host permission removes the origin check but not the preflight, the dev server accepts any Host header and never runs in a packaged extension, the appended connect-src entries are removed for extension build so a tight policy that forgets your API origin passes in dev and fails in production, the browser cannot dial 0.0.0.0 with a 127.0.0.1 fallback, and remote scripts are blocked by extension CSP where no header fixes it. All 5 links resolve under zh-Hans."
    },
    {
      "page": "zh-Hans/docs/features/module-aliases.mdx",
      "claimsChecked": 25,
      "findings": 0,
      "verdict": "accurate",
      "note": "All 5 fences byte-identical: both tsconfig.json paths blocks (with and without baseUrl), the @lib/badge.js import, the plaintext Module not found: Can't resolve '@lib/badge.js' error, and the extension.config.js config hook with path.resolve(process.cwd(), \"src/lib\"). The 6-row injected-alias table matches row for row, including the full specifier lists for React, Preact, Vue, Svelte and the WebAssembly packages (@ffmpeg/core, @imagemagick/magick-wasm, tesseract-wasm). 8 headings in order, 57 inline tokens matching as a multiset, so the 9-extension resolution list (.js, .cjs, .mjs, .jsx, .ts, .mts, .tsx, .json, .svelte) is verbatim. Every negative claim survives without softening: jsconfig.json is not read and its paths block has no effect, Extension.js defines no folder aliases of its own with no built-in @/, ~/ or src/ prefix, tsconfig.json is the alias source even in a plain JavaScript project and the typescript package need not be installed, and the webextension-polyfill$ exception is applied after yours so an alias on that exact specifier does not take effect. All 5 links resolve under zh-Hans, and the two path-resolution links keep their distinct English labels (可预期的路径解析 for Predictable path resolution)."
    },
    {
      "page": "zh-Hans/docs/integrations/biome.mdx",
      "claimsChecked": 27,
      "findings": 0,
      "verdict": "accurate",
      "note": "All 13 fences byte-identical, including the 5-entry CodeGroup install (npm install -D, pnpm add -D, yarn add -D, bun add -d, deno add -D npm:@biomejs/biome), npx biome init, npx biome check, npx biome check --write, both npx biome migrate eslint/prettier --write lines, the package.json scripts block with lint/lint:fix/lint:ci, and both biome.json files with their exact includes arrays [\"**\", \"!!**/dist\"] and [\"**\", \"!!**/dist\", \"!extension-env.d.ts\"]. The 4-row capability table matches. 10 headings in order. Caveats preserved: Biome runs beside the build and never inside it, Extension.js does not read biome.json, does not run Biome, and does not copy the config into the packaged output, there is no Biome template while ESLint, Prettier and Stylelint have one, Biome writes no scripts into package.json, biome ci reports the same findings as check and never edits a file, extension-env.d.ts is rewritten on every dev and build run so formatting it is wasted work, and the closed root-file list the build actually reads (manifest.json, package.json, extension.config.js, tsconfig.json, the PostCSS and Tailwind configs, the .env family) is complete. The --zip-source packaging detail survives. All 4 links resolve under zh-Hans."
    },
    {
      "page": "zh-Hans/docs/languages-and-frameworks/extension-api-types.mdx",
      "claimsChecked": 30,
      "findings": 2,
      "verdict": "minor-issues",
      "note": "All 15 fences byte-identical, including both triple-slash reference blocks, the two TS2304/TS2503 error lines with their exact file positions (src/background.ts(19,1) and src/content/scripts.ts(87,30)), npx tsc --noEmit, the tsconfig.json include/exclude block, and both 5-entry CodeGroups for @types/chrome and @types/webextension-polyfill. Both tables keep their rows: 3 reference rows and 4 symptom/cause/fix rows with their exact English message strings. 7 headings in order. Caveats survive: SWC strips types and never checks them so the build already succeeded before the install, extension-env.d.ts is regenerated on every run and must not be edited, Extension.js does not install @types/chrome and the templates do not declare it, a tsconfig.json that Extension.js writes for a project that had none carries no include array so TypeScript finds the file anyway while an include array of your own that omits it breaks asset imports and the browser global, and the Extension.js build does not fail on type errors. All 4 links resolve under zh-Hans. Two defects fixed. The heading rendered \"When you write browser.* instead\" as 当你改写 browser.\\* 时, which reads as rewriting the namespace rather than choosing it over chrome.*, now 当你改用. And both cross-browser-compatibility link labels read 跨浏览器兼容, against the 跨浏览器兼容性 form used by 20 other zh-Hans links including from-wxt and tabs in this same batch, now aligned."
    },
    {
      "page": "zh-Hans/docs/languages-and-frameworks/bun.mdx",
      "claimsChecked": 24,
      "findings": 0,
      "verdict": "accurate",
      "note": "All 7 fences byte-identical: bunx extension@latest create my-extension --template=content, the plaintext Next steps block (kept in English as real CLI output, matching the house treatment of CLI copy on the tabs and cross-origin pages), the packageManager pin \"bun@1.2.13\", bun install, bun run build, bunx --bun extension@latest --version, and the full plaintext refusal message including \"Bun 1.2.13 emulates Node.js 22.6.0, below the required 22.12\". The 4-row Bun capability table matches, with the last row keeping the flat No: 不行，必须用 Node. 8 headings in order. Every caveat survives: the CLI itself runs on Node and both runtimes must be installed, the engines floor is \"node\": \">=22.12\" checked against process.versions.node at startup, plain bunx respects the #!/usr/bin/env node shebang while --bun trips the guard, the three detection signals (bun.lock or bun.lockb, a packageManager field naming bun, npm_config_user_agent) are all present, the PATH probe order pnpm/yarn/bun is preserved, automatic installs pass --ignore-scripts so a dependency postinstall does not run, Bun.file and other Bun runtime APIs do not belong in extension code, and extension create strips bun.lock and bun.lockb from an imported template. All 3 links resolve under zh-Hans."
    }
  ]
}
