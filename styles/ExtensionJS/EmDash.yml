# AGENTS.md, Conventions > Punctuation: no em dashes.
# Documented since the style guide was written, never enforced.
extends: existence
message: "Do not use em dashes. Use a comma, a colon, or parentheses."
level: error
nonword: true
tokens:
  - '—'
